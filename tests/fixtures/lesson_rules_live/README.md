`s2-groq-response.json` is a verbatim `openai/gpt-oss-120b` (Groq) lesson-extraction response, captured 2026-09-26 by
replaying the DEMO-CC-UPLIFT-1 Stage 2a S2 transcript through `SessionLessonExtractor` (2 of 2 live runs typed the
`RV` + original-ID bank rule as `observation`). It pins the failure the fake-LLM tests could not see: a hard rule
arriving as an observation must still become a lesson.
